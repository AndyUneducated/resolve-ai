"""Defense-in-depth · 4 layers — input / exec / output / memory。

Design document · Decision 4: a single layer can be bypassed; independently
stacked layers are required to increase attack cost exponentially.
"""
